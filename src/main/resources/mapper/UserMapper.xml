<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.campusblog.mapper.UserMapper">
    <!-- 配置查询结果的列名和实体类的属性名的对应关系 -->
    <resultMap id="BaseResultMap" type="com.zs.campusblog.entity.User">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="gender" jdbcType="TINYINT" property="gender" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="phone" jdbcType="VARCHAR" property="phone" />
        <result column="avatar" jdbcType="VARCHAR" property="avatar" />
        <result column="description" jdbcType="VARCHAR" property="description" />
        <result column="enabled" jdbcType="TINYINT" property="enabled" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>
    <resultMap id="lazyLoadRoles" type="com.zs.campusblog.entity.User" extends="BaseResultMap">
        <collection property="roles" ofType="com.zs.campusblog.entity.Role"
                    select="com.zs.campusblog.mapper.UserMapper.getRolesByUserId"
                    column="id">
        </collection>
    </resultMap>
    <select id="getUserByUsername" resultMap="lazyLoadRoles">
        select * from user where username=#{username}
    </select>
    <select id="getRolesByUserId" resultType="com.zs.campusblog.entity.Role">
        select r.* from user_role ur, role r where ur.role_id = r.id and ur.user_id=#{id}
    </select>
    <insert id="addUser">
        insert into user set username=#{username},password=#{password}
    </insert>
</mapper>